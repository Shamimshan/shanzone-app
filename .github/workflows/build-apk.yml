name: Build APK

# GitHub ke "Actions" tab se manually run karein ("Run workflow" button),
# ya har baar jab aap "main" branch pe push karein toh apne aap chalega.
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: Generate missing Android platform files
        run: flutter create . --platforms=android --project-name shanzone_app

      - name: Bump Kotlin Gradle plugin version (fixes plugin compatibility error)
        run: sed -i '/org.jetbrains.kotlin.android/ s/version "[0-9.]*"/version "1.9.24"/' android/settings.gradle

      - name: Get dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: shanzone-app-release
          path: build/app/outputs/flutter-apk/app-release.apk
